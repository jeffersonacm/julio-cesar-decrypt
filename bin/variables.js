const variables = {
    Api: {
        port: process.env.port || 3000
    },
    Codenation: {
        token: process.env.port || 'your_token',
		api:   process.env.port || "link_api"
    }
}

module.exports = variables;